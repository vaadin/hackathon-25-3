# Feature 12: AI

## Overview

The assistant, specified in full in `06-ai.md`. This document holds the behaviour that has to be tested.

Covers B2, B3, K8, A13.

## Behaviour

### Counter order form, route `/orders/new`

A form for an order taken by the bakery's own staff, plus an assistant panel. The barista pastes what the customer said, or attaches a photograph of a handwritten note, and one button hands both to the model, which fills the form. How the order arrived is a field on the form, `PHONE`, `EMAIL` or `COUNTER`, and it is the barista's: it sits outside the container the controller walks. Field rules, marker content, source tracking and confidence are as specified in `06-ai.md`.

### Order board assistant

A panel beside the board, at `/orders/ask`, that answers a question by filling a grid of its own from a query. `GridAIController` owns the grid it fills, so the board keeps its columns, its sorting and its selection, and the answer arrives next to it with whatever columns the question turned out to need. Questions it must answer in the demo: which orders are at risk of being late, which customer ordered the most this week, which orders have allergen comments.

### Dashboard assistant

A question turns into a chart through `ChartAIController`, in a fifth chart below the four fixed panels. The four answer what a bakery asks every morning; this one answers the question nobody wrote a panel for.

### What the assistant may query

Both controllers work by letting the model write SQL, which is a different kind of trust from letting it fill a text field, and the answer is not a sentence in a prompt. `BakeryDatabase` exposes three read-only views, `ai_orders`, `ai_order_lines` and `ai_products`, holding what a question about orders, products and revenue needs and nothing else: no tracking token, no password hash, no internal note, no address, no telephone number, no attachment. On top of that it accepts one statement, that statement a `SELECT` over those three names, capped at 500 rows and timed out at five seconds. A query that succeeds completely still cannot read anything private.

A database user with select only grants is the version that does not depend on that class being right. It needs a second data source and a different story on H2 and on PostgreSQL, and it is the first thing to add if this ever faces anything but a demo.

### Policy and meter

`RequestInterceptor` masks card shaped strings, rejects off topic prompts with a readable sentence, and postpones over budget prompts by dropping the oldest history. `ResponseMetadata` feeds a small panel with tokens, estimated cost and finish reason. A `max_tokens` finish tells the user the answer was cut off.

### Providers

One provider, the real one, under the `ai` profile with a key present. Without it the assistant is off and says so: a warning in the log and a red panel where the question box would have been. Nothing answers from a recording.

## Edge cases

| Scenario | Behaviour |
| --- | --- |
| The model proposes a product that does not exist | The write is rejected by the binder and reported back. The model corrects itself on the next turn |
| The model proposes a full slot | Rejected with the slot named, and the assistant proposes the next free one |
| The pasted text contains a card number | Masked before the request leaves, with a visible warning |
| The prompt asks about staff salaries | Rejected with a sentence, no request sent |
| A tool throws | `ToolException` gives the model a safe message, the stack trace stays in the log |
| The network dies mid demo | The turn fails and the panel says so. The form is still a form and the order can be saved by hand |
| A turn fills the lines and leaves the pickup empty | The screen says so in a notification, and Save refuses until a slot is chosen. The assistant's own account of the pickup is not evidence that a field holds one |
| A photograph is attached with nothing typed | The turn is sent with a prompt saying to read the attached note. Both empty is the only case that asks instead of guessing |
| No key, or no `ai` profile | Every assistant panel is red and says the assistant is off. Everything else on those screens works |

## Acceptance criteria

### AC1: Filling works and is honest
- [x] Pasting the reference message fills name, phone, both product lines and the slot
- [ ] Every filled field carries a marker whose popover shows the source snippet and the confidence
- [x] Editing a field clears its marker

### AC2: The model cannot break the rules
- [x] It cannot write into a hidden field
- [x] It cannot create a customer
- [x] It cannot choose a product that does not exist or a quantity out of range
- [x] It cannot book a full slot

### AC3: The policy layer holds
- [x] A card shaped string is masked before the request
- [x] An off topic prompt is rejected with a readable message and no request is sent

### AC4: The meter is accurate
- [x] Every turn adds its token usage to the panel
- [x] A truncated answer shows the cut off warning

### AC6: The live provider is real
- [x] Built with `-Pai` and a key present, the application constructs a `SpringAILLMProvider` over the auto configured OpenAI model and says so on the about page and over the assistant panel
- [x] The same build with no key boots, with the assistant off, rather than failing at startup
- [x] Each orchestrator is handed a provider of its own
- [x] Every test that needs a turn really calls OpenAI, and all of them are excluded from every default run by their tag

### AC5: It is honest when there is no model
- [x] With no key the application boots, every view opens and nothing throws
- [x] Every assistant panel shows a disabled state a person can see, and the startup log says why
- [x] Asking for a session when there is none fails loudly rather than falling back
- [x] With no model the form is still a form: a barista fills it by hand and a phone order lands

### Still open

- Source tracking produces nothing from a real model. `setSourceTrackingEnabled(true)` is on, the popover renders a source correctly when one exists, and neither `gpt-4o-mini` nor `gpt-4o` has ever reported one: `getFieldSource` is empty for every field after every live turn. AC1's second criterion therefore fails twice over, once because the model does not supply the data and once because the badge that would show it does not open. Both are in `FEEDBACK-25.3.md`.
- AI-02 and AI-03 have no test. There is nothing to assert while no source arrives, and the only honest place for one is the live tier, against a model that has never produced the data.
- The marker popover cannot be opened by a person. The content is built, translated and correct, and forcing the popover open in a browser shows the snippet, the confidence and the revert control exactly as this document asks. Clicking the badge does nothing, which is a bug in the marker component rather than in this application: see the row in `FEEDBACK-25.3.md`. The criterion stays unticked because what it promises is that somebody can read it.
- Both controllers are text to SQL, so the earlier wording of AI-09, "the answer names only orders visible in the grid", was never something they could keep. The criterion now says what they can: the answer comes from the three views and from nothing else. The wording changed rather than the test being written to a promise the platform does not make.
- The three demo questions are asserted against a real model, by shape and never by wording. A live turn writes `current_date`, and the live profile shifts the dataset onto the wall clock so that it finds something.

## Test cases

| Id | Given | When | Then | Tier | Verified by |
| --- | --- | --- | --- | --- | --- |
| AI-01 | The phone order form and a real model | Pasting the reference message | Name, phone, two lines and the slot are filled | live model, tagged `live-ai` | `FormFillingBrowserlessTest` |
| AI-02 | A filled field | Opening its marker popover | The source snippet and the confidence are shown | live model, tagged `live-ai` | `FormFillingBrowserlessTest` |
| AI-03 | A filled field | Typing over it | The marker clears | live model, tagged `live-ai` | `FormFillingBrowserlessTest` |
| AI-04 | A tool call proposing a product that does not exist | Running it | The write is rejected and the refusal names the catalogue | browserless | `FormFillingGuardrailsBrowserlessTest` |
| AI-13 | The phone order form | Reading the form state the model is given | The staff only note is not in it, and the fields it may fill are | browserless | `FormFillingGuardrailsBrowserlessTest` |
| AI-05 | A tool call proposing a full slot | Running it | Rejected with the slot named | browserless | `FormFillingGuardrailsBrowserlessTest` |
| AI-06 | A message containing a card number | Sending it | The outgoing request carries the masked form | unit | `AssistantPolicyTest` |
| AI-07 | An off topic prompt | Sending it | Rejected, no request issued | unit | `AssistantPolicyTest` |
| AI-08 | A turn that finished on the token limit | Recording it in the meter | The cut off warning is shown | browserless | `PhoneOrderBrowserlessTest` |
| AI-09 | The order board | Asking which orders are at risk | The grid fills from the orders, and a rendered row carries a reference and a pickup time | browserless | `GridAssistantBrowserlessTest` |
| AI-14 | The order board assistant | Asking something that would need a private table | The query is refused by the database and the grid stays empty | browserless | `GridAssistantBrowserlessTest` |
| AI-15 | The dashboard | Asking for revenue by channel | A chart is drawn from a query, in styled mode, titled with the question | browserless | `ChartAssistantBrowserlessTest` |
| AI-16 | The assistant's database | Any statement that is not a single SELECT over the three views | Refused, with a message naming what it could have read | unit | `BakeryDatabaseTest` |
| AI-10 | No key | Opening the phone order form | The panel says the assistant is off, nothing offers to fill the form, and an order saved by hand is recorded as a phone order | browserless | `AssistantOffBrowserlessTest`, `PhoneOrderBrowserlessTest` |
| AI-11 | The real provider | Asking it something with one right answer | It answers, and reports the tokens the turn cost | unit, tagged `live-ai`, excluded from CI | `LiveAssistantTest` |
| AI-12 | The `ai` profile with no key | Starting the application | It boots with the assistant off, and the log and the panel both name which switch is off | unit | `AssistantConfigurationTest` |
