# The reproducer projects ship as zips under projects/. An unzipped working
# copy is scratch: it must not come back into the repository, because a reader
# who opens an issue wants one file to download, not a tree to browse.
*/
!projects/
projects/*
!projects/*.zip
