global:
  scrape_interval: 5s

scrape_configs:
  - job_name: bakery
    metrics_path: /actuator/prometheus
    static_configs:
      # host.docker.internal reaches the application running outside Docker
      - targets: ["host.docker.internal:8080"]
